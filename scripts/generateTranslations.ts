import fs from 'fs';
import path from 'path';

// Define the API Key here for the script execution, but it should be disabled afterwards
// Google Translate API Key
const API_KEY = ""; // HIDDEN AFTER USE
// generate(); // DISABLED AFTER USE

